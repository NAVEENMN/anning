import SwiftUI

enum EditorStyles {
    static let cardBackground = Color.black.opacity(0.03)
    static let border = Color.secondary.opacity(0.18)
    static let subtleBorder = Color.secondary.opacity(0.12)
}
